# Custom Modules

Drop your own site-specific scripts here. Any `.ps1` file with a `$Tool`
metadata block (see any script under `Modules/` for the pattern) will
automatically appear in the menu — no other changes needed.
